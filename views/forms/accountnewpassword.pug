if error
    .alert.alert-danger Invalid inputs
form(method='post' action="/reset-password")
    fieldset
        .form-row.mb-3
            .col
                .form-group
                    input#password.form-control(type='password' name='password' maxlength='100')
                    label.control-label New Password
            .col
                .form-group
                    input#password.form-control(type='password' name='password' maxlength='100')
                    label.control-label Confirm New Password
        .text-center.mb-5
            button.btn.btn-lg.btn-primary(type='submit') Set new password
